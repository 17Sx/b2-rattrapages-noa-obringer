{% extends 'base.html.twig' %}

{% block title %}Mongoo -
	{% if is_granted('ROLE_USER') %}Administration{% else %}Accueil
	{% endif %}
{% endblock %}

{% block body %}
	<div class="row">
		<div class="col-12 text-center mb-4">
			{% if is_granted('ROLE_USER') %}
				<h1 class="display-4" style="color: var(--primary-color);">Bienvenue dans l'administration Mongoo</h1>
				<p class="lead">Gérez vos restaurants et vos employés en toute simplicité</p>
			{% else %}
				<h1 class="display-4" style="color: var(--primary-color);">Bienvenue chez Mongoo</h1>
				<p class="lead">Découvrez nos restaurants et notre concept unique de restauration rapide et saine</p>
			{% endif %}
		</div>
	</div>

	<div class="row mt-4">
		<div class="col-md-6">
			<div class="card mb-4">
				<div class="card-body text-center">
					<h3 style="color: var(--primary-color);">Restaurants</h3>
					{% if is_granted('ROLE_USER') %}
						<p>Gérez les restaurants Mongoo</p>
						<div class="d-grid gap-2">
							<a href="{{ path('app_restaurant_index') }}" class="btn btn-primary">Gérer les restaurants</a>
							<a href="{{ path('app_restaurant_new') }}" class="btn btn-outline-primary">
								<i class="fas fa-plus"></i>
								Ajouter un restaurant
							</a>
						</div>
					{% else %}
						<p>Découvrez nos restaurants Mongoo</p>
						<a href="{{ path('app_restaurant_index') }}" class="btn btn-primary">Voir nos restaurants</a>
					{% endif %}
				</div>
			</div>
		</div>
		{% if is_granted('ROLE_USER') %}
			<div class="col-md-6">
				<div class="card mb-4">
					<div class="card-body text-center">
						<h3 style="color: var(--secondary-color);">Employés</h3>
						<p>Gérez les employés Mongoo</p>
						<div class="d-grid gap-2">
							<a href="/employee" class="btn btn-secondary">Gérer les employés</a>
							<a href="/employee/new" class="btn btn-outline-secondary">
								<i class="fas fa-plus"></i>
								Ajouter un employé
							</a>
						</div>
					</div>
				</div>
			</div>
		{% endif %}
	</div>

	<div class="row mt-4">
		<div class="col-12">
			<div class="card">
				<div class="card-body">
					<h4 style="color: var(--primary-color);">À propos de Mongoo</h4>
					<p>Mongoo est une chaîne de restauration rapide spécialisée dans la salade à composer. Avec 8 restaurants dans Paris et sa région, nous offrons une expérience unique de restauration rapide et saine.</p>
					{% if not is_granted('ROLE_USER') %}
						<div class="mt-3">
							<p>Vous êtes un administrateur ?
								<a href="{{ path('app_login') }}">Connectez-vous</a>
								ou
								<a href="{{ path('app_register') }}">inscrivez-vous</a>
								pour gérer les restaurants.</p>
						</div>
					{% endif %}
				</div>
			</div>
		</div>
	</div>
{% endblock %}
